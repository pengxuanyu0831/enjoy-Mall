server:
  port: 8194
  tomcat:
    max-threads: 1000
    accept-count: 1000
    max-connections: 2000
    uri-encoding: UTF-8

spring:
  profiles:
    active: ${spring.profiles.active:test}
  application:
    name: mall-dashboard